// ignore_for_file: non_constant_identifier_names
enum ScreenType { PHONE, PHABLET, TABLET, DESKTOP }

enum Platform { iOS, Android, Web }

enum UserRole { CUSTOMER, DESIGNER }

enum ProjectStatus { DONE, IN_PROGRESS, CANCELED }

enum RequestStatus { AVAILABLE, TAKEN }

enum ResourceWorkStatus { IN_PROGRESS, DECLINED, FINAL }

enum AspectRatioType { LONG, SHORT }

enum AspectRatio { Aspect16_9, Aspect19_9 }

enum PopupMode { Info, Warning, Error }

const DEFAULT_APPLICATION_MESSAGE =
    'Chào bạn, sau khi đọc kỹ qua các yêu cầu, tôi nghĩ mình chính là ứng viên mà bạn đang tìm kiếm, rất hy vọng được hợp tác cùng bạn.';
