@page
@model WebApplication.Pages.Areas.Customers.Workspaces.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Workspaces</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Workspace[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workspace[0].CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workspace[0].UpdatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workspace[0].DeletedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workspace[0].ObjectId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Workspace) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpdatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeletedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ObjectId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="../Discussions/Index" asp-route-workspaceId="@item.Id">Discussion</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
